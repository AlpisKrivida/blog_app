{
    "configurations": [
        {
            "name": "debug graphql",
            "program": "${workspaceFolder}/graphql/app.js",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }

    ]
}